---
phase: quick-28-add-moonshine-and-sensevoice-to-benchmar
task: GPU acceleration patch (follow-up)
total_tasks: 1
status: not_started
last_updated: "2026-03-03T18:45:07.743Z"
---

<current_state>
Quick task 28 is COMPLETE — Moonshine (tiny, base) and SenseVoice added to benchmark binary
behind `bench_extra` feature flag via transcribe-rs 0.2.8. All models downloaded. Verification passed.

HOWEVER: transcribe-rs hardcodes `CPUExecutionProvider` for both Moonshine and SenseVoice.
This means the benchmark runs those models on CPU while Whisper and Parakeet run on GPU —
an apples-to-oranges comparison. User confirmed they want GPU acceleration added.
</current_state>

<completed_work>

- Quick task 28: Added transcribe-rs dependency with moonshine + sense_voice features behind `bench_extra` flag
- Cargo.toml updated with `bench_extra = ["dep:transcribe-rs"]`
- benchmark.rs has Moonshine tiny, Moonshine base, SenseVoice sections behind `#[cfg(feature = "bench_extra")]`
- All 3 model sets downloaded to `%APPDATA%/VoiceType/models/`:
  - `moonshine-tiny-ONNX/` (108 MB: encoder_model.onnx, decoder_model_merged.onnx, tokenizer.json)
  - `moonshine-base-ONNX/` (240 MB: encoder_model.onnx, decoder_model_merged.onnx, tokenizer.json)
  - `sensevoice-small/` (229 MB: model.onnx [int8], tokens.txt)
- test-fixtures/BENCHMARK.md updated with extended model docs
- Verification passed (4/4 must_haves), committed as 4c2998e
- Research completed: Moonshine v2, SenseVoice, Canary 180M Flash, Parakeet v3, and others evaluated

</completed_work>

<remaining_work>

- Patch benchmark.rs to bypass transcribe-rs CPU-only sessions and load Moonshine/SenseVoice
  ONNX models directly with `ort` using CUDA/DirectML execution providers (same pattern as
  parakeet-rs). The model files are identical — only the session init needs to change.
- Alternative: load models directly with ort in benchmark.rs instead of using transcribe-rs
  engine wrappers. This avoids the CPUExecutionProvider limitation entirely.
- Run the actual benchmark once GPU acceleration is working
- Optionally: update BENCHMARK.md with real benchmark results

</remaining_work>

<decisions_made>

- transcribe-rs 0.2.8 chosen (not 0.1.5) — features didn't exist in 0.1.5
- ort version compatible: transcribe-rs uses ort = "2.0.0-rc.10", same as parakeet-rs
- MoonshineModelParams::tiny()/base() convenience constructors used (not ModelVariant enum)
- SenseVoice int8 model downloaded (239 MB) instead of fp32 (938 MB) — ort handles quantized models transparently
- Feature flag `bench_extra` keeps everything isolated from main app code

</decisions_made>

<blockers>

- transcribe-rs hardcodes CPUExecutionProvider in both moonshine/model.rs and sense_voice/model.rs
- Fix: Either (a) bypass transcribe-rs and use ort directly in benchmark.rs with CUDAExecutionProvider,
  or (b) patch transcribe-rs locally (like existing esaxx-rs and parakeet-rs patches)

</blockers>

<context>
Session started as model research — user asked what other models to benchmark beyond current
Whisper + Parakeet. Three parallel research agents identified Moonshine v2 and SenseVoice as
top candidates. User approved adding both to the benchmark via /gsd:quick --full.

The GPU acceleration gap was discovered late when checking transcribe-rs source. User wants
this fixed before running actual benchmarks. The approach should either bypass transcribe-rs
for session creation or patch it locally.

Key files for the GPU fix:
- src-tauri/src/bin/benchmark.rs (lines 627-835 — the bench_extra section)
- transcribe-rs source: engines/moonshine/model.rs and engines/sense_voice/model.rs
  both use `CPUExecutionProvider::default().build()` in `init_session()`
- For reference: src-tauri/src/transcribe_parakeet.rs shows how parakeet-rs uses CUDA EP
</context>

<next_action>
Start with: Patch benchmark.rs to load Moonshine and SenseVoice ONNX models directly via ort
with CUDAExecutionProvider (falling back to DirectML then CPU). Replace the transcribe-rs
engine wrappers in the bench_extra section with direct ort Session::builder() calls. The
tokenizer (Moonshine) and token vocab (SenseVoice) loading can still use transcribe-rs
utilities, or be reimplemented inline since the formats are simple (JSON BPE / line-delimited text).
</next_action>
