---
phase: 20.1-implement-the-vad-chunking-method
verified: 2026-03-06T23:00:00Z
status: gaps_found
score: 5/5 must-haves verified
gaps:
  - truth: "VAD-01 through VAD-06 requirement IDs are referenced but not defined in REQUIREMENTS.md"
    status: failed
    reason: "ROADMAP.md and PLAN frontmatter reference requirements VAD-01 through VAD-06, but REQUIREMENTS.md has no VAD section. These IDs are orphaned -- no definition exists to verify against."
    artifacts: []
    missing:
      - "Add VAD requirements section to .planning/REQUIREMENTS.md with VAD-01 through VAD-06 definitions"
      - "Add VAD-01 through VAD-06 to the Traceability table in REQUIREMENTS.md mapped to Phase 20.1"
---

# Phase 20.1: VAD Chunking Generalization Verification Report

**Phase Goal:** Generalize the Moonshine-specific VAD chunking algorithm into an engine-agnostic function and wire it into Whisper and Parakeet engine dispatch, so all three engines handle 60s+ recordings via VAD-based chunk splitting at silence boundaries.
**Verified:** 2026-03-06T23:00:00Z
**Status:** gaps_found
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Whisper transcription of 60s+ audio produces correct, complete text via VAD chunking | VERIFIED | `pipeline.rs:190-212` -- Whisper arm calls `vad::vad_chunk_audio(&samples, 30)` when `samples.len() > 30*16000`, iterates chunks inside `spawn_blocking`, space-joins results |
| 2 | Parakeet transcription of 60s+ audio produces correct, complete text via VAD chunking | VERIFIED | `pipeline.rs:259-282` -- Parakeet arm has identical chunking pattern with `vad::vad_chunk_audio(&samples, 30)` before `spawn_blocking` |
| 3 | Moonshine transcription continues to work identically (uses generalized function) | VERIFIED | `transcribe_moonshine.rs:120` calls `crate::vad::vad_chunk_audio(audio, 30)` directly; `vad.rs:166-168` `vad_chunk_for_moonshine` delegates to `vad_chunk_audio(samples, 30)` as backward-compatible wrapper |
| 4 | Audio <=30s bypasses chunking entirely for all engines (zero overhead) | VERIFIED | All three paths use `if samples.len() > CHUNK_THRESHOLD_SAMPLES` / `if audio.len() > MOONSHINE_MAX_SAMPLES` with else branch wrapping in `vec![samples]` -- no VAD invocation for short audio |
| 5 | No new dependencies added to Cargo.toml | VERIFIED | `git diff 668523d^..7577b67 -- src-tauri/Cargo.toml` produces no output -- Cargo.toml untouched |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/vad.rs` | Generic `pub fn vad_chunk_audio` | VERIFIED | Lines 183-286: full implementation with parameterized `max_segment_secs`, VAD classification, silence-boundary splitting, min/max segment enforcement, fallback |
| `src-tauri/src/pipeline.rs` | Chunk loop dispatch for Whisper and Parakeet | VERIFIED | Lines 188-212 (Whisper) and 257-282 (Parakeet): VAD chunking before `spawn_blocking`, chunk iteration with space-joined concatenation |
| `src-tauri/src/transcribe_moonshine.rs` | Moonshine using generic `vad_chunk_audio` | VERIFIED | Line 120: calls `crate::vad::vad_chunk_audio(audio, 30)` instead of old `vad_chunk_for_moonshine` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `pipeline.rs` | `vad.rs` | `vad::vad_chunk_audio(&samples, 30)` | WIRED | Lines 196 and 265 in pipeline.rs; `use crate::vad;` at line 4 |
| `transcribe_moonshine.rs` | `vad.rs` | `crate::vad::vad_chunk_audio(audio, 30)` | WIRED | Line 120 in transcribe_moonshine.rs uses full crate path |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| VAD-01 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |
| VAD-02 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |
| VAD-03 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |
| VAD-04 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |
| VAD-05 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |
| VAD-06 | 20.1-01-PLAN | (undefined in REQUIREMENTS.md) | ORPHANED | No definition exists in REQUIREMENTS.md |

All six VAD requirement IDs are referenced in the ROADMAP and PLAN but never defined in REQUIREMENTS.md. The implementation itself is complete and correct -- the gap is purely a documentation/traceability issue.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No anti-patterns found |

No TODOs, FIXMEs, placeholders, empty implementations, or console-only handlers found in the three modified files.

### Human Verification Required

### 1. Whisper long-audio transcription accuracy

**Test:** Record or process a 60s+ audio clip using Whisper engine; verify output is coherent and complete.
**Expected:** Text from all portions of the recording appears in correct order, space-joined between chunks.
**Why human:** Transcription quality and chunk-boundary artifacts (word splits, duplications) cannot be verified by code inspection alone.

### 2. Parakeet long-audio transcription accuracy

**Test:** Same as above with Parakeet engine selected.
**Expected:** Complete, correctly ordered transcription text.
**Why human:** Same reason -- runtime inference quality.

### 3. VAD chunking before spawn_blocking constraint

**Test:** Verify via log output that "splitting with VAD" log line appears before spawn_blocking enters.
**Expected:** Log line `Pipeline: Whisper audio X.Xs exceeds 30s -- splitting with VAD` appears, followed by chunk segment logs, then inference logs.
**Why human:** Runtime execution ordering verification from logs.

### Gaps Summary

The implementation is complete and all five observable truths are verified in the codebase. The only gap is a documentation/traceability issue: REQUIREMENTS.md does not define VAD-01 through VAD-06, even though the ROADMAP and PLAN reference these IDs. This means there is no formal requirement text to verify the implementation against -- the requirement IDs are orphaned references.

The commits (668523d, 7577b67) are verified to exist with the expected file changes. No new dependencies were added. No anti-patterns were found.

---

_Verified: 2026-03-06T23:00:00Z_
_Verifier: Claude (gsd-verifier)_
