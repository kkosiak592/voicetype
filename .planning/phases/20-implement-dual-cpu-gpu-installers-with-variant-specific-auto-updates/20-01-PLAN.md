---
phase: 20-implement-dual-cpu-gpu-installers-with-variant-specific-auto-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/tauri.cpu.conf.json
  - src-tauri/tauri.gpu.conf.json
  - src-tauri/windows/gpu-hooks.nsh
  - src-tauri/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "CPU build compiles without linking any CUDA DLLs (cudart64_12.dll absent from binary imports)"
    - "GPU build compiles with CUDA enabled on both whisper-rs and parakeet-rs"
    - "Each variant's binary points to its own updater endpoint (latest-cpu.json or latest-gpu.json)"
    - "GPU installer warns when no NVIDIA GPU is detected via NSIS pre-install hook"
    - "get_build_variant IPC command returns 'CPU' or 'GPU' based on compile-time feature"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "cuda feature flag with weak dependency composition"
      contains: "cuda = [\"whisper-rs?/cuda\", \"parakeet-rs?/cuda\"]"
    - path: "src-tauri/tauri.cpu.conf.json"
      provides: "CPU variant config overlay with updater endpoint"
      contains: "latest-cpu.json"
    - path: "src-tauri/tauri.gpu.conf.json"
      provides: "GPU variant config overlay with updater endpoint, CUDA resources, NSIS hooks"
      contains: "latest-gpu.json"
    - path: "src-tauri/windows/gpu-hooks.nsh"
      provides: "NSIS pre-install NVIDIA driver check"
      contains: "NSIS_HOOK_PREINSTALL"
    - path: "src-tauri/src/lib.rs"
      provides: "get_build_variant Tauri command"
      contains: "fn get_build_variant"
  key_links:
    - from: "src-tauri/Cargo.toml"
      to: "whisper-rs, parakeet-rs"
      via: "cuda feature composition"
      pattern: "whisper-rs\\?/cuda"
    - from: "src-tauri/tauri.gpu.conf.json"
      to: "src-tauri/windows/gpu-hooks.nsh"
      via: "bundle.windows.nsis.installerHooks"
      pattern: "installerHooks"
---

<objective>
Restructure Cargo features for CPU/GPU split, create config overlays with variant-specific updater endpoints, add NSIS GPU driver check, and expose build variant to frontend.

Purpose: All local source changes needed for dual-variant builds. After this plan, `cargo tauri build` produces a CPU binary and `cargo tauri build --features cuda --config src-tauri/tauri.gpu.conf.json` produces a GPU binary, each pointing to its own update channel.

Output: Modified Cargo.toml, tauri.conf.json, two new config overlays, NSIS hook file, get_build_variant command in lib.rs
</objective>

<execution_context>
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-implement-dual-cpu-gpu-installers-with-variant-specific-auto-updates/20-CONTEXT.md
@.planning/phases/20-implement-dual-cpu-gpu-installers-with-variant-specific-auto-updates/20-RESEARCH.md
@src-tauri/Cargo.toml
@src-tauri/tauri.conf.json
@src-tauri/src/lib.rs

<interfaces>
<!-- Existing Cargo.toml feature structure (to be modified): -->
```toml
[features]
default = ["whisper", "parakeet"]
whisper = ["dep:whisper-rs", "dep:nvml-wrapper"]
parakeet = ["dep:parakeet-rs"]

[dependencies]
whisper-rs = { version = "0.15", features = ["cuda"], optional = true }
parakeet-rs = { version = "0.1.9", features = ["cuda", "directml"], optional = true }
```

<!-- Existing tauri.conf.json updater config (to be modified): -->
```json
{
  "plugins": {
    "updater": {
      "endpoints": [
        "https://github.com/kkosiak592/voicetype/releases/latest/download/latest.json"
      ]
    }
  }
}
```

<!-- Existing generate_handler registration (lib.rs ~line 1528): -->
```rust
builder.invoke_handler(tauri::generate_handler![
    rebind_hotkey,
    unregister_hotkey,
    // ... many commands ...
    #[cfg(all(feature = "whisper", debug_assertions))]
    force_cpu_transcribe,
])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Cargo features and config overlays for CPU/GPU split</name>
  <files>src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/tauri.cpu.conf.json, src-tauri/tauri.gpu.conf.json, src-tauri/windows/gpu-hooks.nsh</files>
  <action>
**Cargo.toml changes:**

1. Add a new `cuda` feature using the Cargo weak dependency composition syntax (`?/`):
```toml
[features]
default = ["whisper", "parakeet"]
whisper = ["dep:whisper-rs", "dep:nvml-wrapper"]
parakeet = ["dep:parakeet-rs"]
cuda = ["whisper-rs?/cuda", "parakeet-rs?/cuda"]
```

2. Remove `features = ["cuda"]` from the `whisper-rs` dependency declaration. It becomes:
```toml
whisper-rs = { version = "0.15", optional = true }
```

3. Remove `"cuda"` from the `parakeet-rs` features list (keep `"directml"`). It becomes:
```toml
parakeet-rs = { version = "0.1.9", features = ["directml"], optional = true }
```

4. Update the comments on whisper-rs and parakeet-rs to reflect that CUDA is now controlled by the `cuda` feature flag, not hardcoded.

**tauri.conf.json changes:**

Remove the `plugins.updater.endpoints` array from the base config entirely. Each variant overlay provides its own endpoint. The base config retains the `pubkey` and `createUpdaterArtifacts` settings. The result:
```json
"plugins": {
  "updater": {
    "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEIxQzY0OUEwODM2REI4N0UKUldSK3VHMkRvRW5Hc2Z3T3hZODRDdXFFSDFBR0JFZXI2cGdUR2RQdktreFdVT0ZJeTNFRVBpa0kK"
  }
}
```

**Create src-tauri/tauri.cpu.conf.json** (new file — JSON Merge Patch overlay):
```json
{
  "plugins": {
    "updater": {
      "endpoints": [
        "https://github.com/kkosiak592/voicetype/releases/latest/download/latest-cpu.json"
      ]
    }
  }
}
```

**Create src-tauri/tauri.gpu.conf.json** (new file — JSON Merge Patch overlay):
```json
{
  "plugins": {
    "updater": {
      "endpoints": [
        "https://github.com/kkosiak592/voicetype/releases/latest/download/latest-gpu.json"
      ]
    }
  },
  "bundle": {
    "resources": {
      "cuda-libs/cudart64_12.dll": "cudart64_12.dll",
      "cuda-libs/cublas64_12.dll": "cublas64_12.dll",
      "cuda-libs/cublasLt64_12.dll": "cublasLt64_12.dll"
    },
    "windows": {
      "nsis": {
        "installerHooks": "./windows/gpu-hooks.nsh"
      }
    }
  }
}
```

**Create src-tauri/windows/gpu-hooks.nsh** (new file — NSIS pre-install hook):
```nsis
!macro NSIS_HOOK_PREINSTALL
  ; Check for NVIDIA display adapter in device class registry
  ReadRegStr $0 HKLM "SYSTEM\CurrentControlSet\Control\Class\{4D36E968-E325-11CE-BFC1-08002BE10318}\0000" "DriverDesc"
  ${If} $0 == ""
    ; Try secondary adapter index (multi-GPU or multi-adapter systems)
    ReadRegStr $0 HKLM "SYSTEM\CurrentControlSet\Control\Class\{4D36E968-E325-11CE-BFC1-08002BE10318}\0001" "DriverDesc"
  ${EndIf}
  ${If} $0 == ""
    MessageBox MB_ICONEXCLAMATION|MB_OK "Warning: No NVIDIA GPU detected.$\n$\nThis is the GPU variant of VoiceType and requires an NVIDIA GPU with drivers installed.$\n$\nInstallation will continue, but CUDA acceleration may not work."
  ${EndIf}
!macroend
```
Warn-only per discretion — do not block installation. Users may install drivers later or use DirectML fallback.

**Add cuda-libs/ to .gitignore** — the `src-tauri/cuda-libs/` directory is populated by CI only, never committed:
Add `src-tauri/cuda-libs/` to the project `.gitignore` file.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q "cuda = \[" Cargo.toml && grep -q "whisper-rs?/cuda" Cargo.toml && ! grep -q 'features = \["cuda"\]' Cargo.toml && echo "Cargo features OK" && test -f tauri.cpu.conf.json && test -f tauri.gpu.conf.json && test -f windows/gpu-hooks.nsh && echo "All overlay files exist" && ! grep -q "latest.json" tauri.conf.json && echo "Base config has no updater endpoint"</automated>
  </verify>
  <done>
    - Cargo.toml has `cuda` feature with `whisper-rs?/cuda` and `parakeet-rs?/cuda` composition
    - whisper-rs and parakeet-rs dependencies no longer have hardcoded `features = ["cuda"]`
    - Base tauri.conf.json has no updater endpoint (removed)
    - tauri.cpu.conf.json exists with `latest-cpu.json` endpoint
    - tauri.gpu.conf.json exists with `latest-gpu.json` endpoint, CUDA resources map, and NSIS hooks reference
    - gpu-hooks.nsh exists with NSIS_HOOK_PREINSTALL macro (warn-only)
    - .gitignore includes src-tauri/cuda-libs/
  </done>
</task>

<task type="auto">
  <name>Task 2: Add get_build_variant Tauri IPC command</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
1. Add a new Tauri command function near the other simple IPC commands (before `run_app`):

```rust
#[tauri::command]
fn get_build_variant() -> &'static str {
    #[cfg(feature = "cuda")]
    { "GPU" }
    #[cfg(not(feature = "cuda"))]
    { "CPU" }
}
```

2. Register `get_build_variant` in the `generate_handler!` macro invocation (line ~1528). Add it after `get_hook_status`:

```rust
builder.invoke_handler(tauri::generate_handler![
    rebind_hotkey,
    unregister_hotkey,
    register_hotkey,
    get_hook_status,
    get_build_variant,  // <-- add here
    set_recording_mode,
    // ... rest unchanged
])
```

This command has no feature gate — it is always available regardless of which features are compiled.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q "fn get_build_variant" src/lib.rs && grep -q "get_build_variant" src/lib.rs && echo "get_build_variant command registered"</automated>
  </verify>
  <done>
    - `get_build_variant()` Tauri command exists in lib.rs
    - Returns "GPU" when compiled with `cuda` feature, "CPU" otherwise
    - Registered in generate_handler! macro
  </done>
</task>

</tasks>

<verification>
1. `grep -c "cuda" src-tauri/Cargo.toml` shows the new feature line (not in dependency features)
2. `cat src-tauri/tauri.conf.json | python -m json.tool` parses valid JSON with no `endpoints` key
3. `cat src-tauri/tauri.cpu.conf.json | python -m json.tool` parses valid JSON with `latest-cpu.json`
4. `cat src-tauri/tauri.gpu.conf.json | python -m json.tool` parses valid JSON with `latest-gpu.json`, resources, and installerHooks
5. `cat src-tauri/windows/gpu-hooks.nsh` contains `NSIS_HOOK_PREINSTALL`
6. `grep get_build_variant src-tauri/src/lib.rs` shows both the function and the handler registration
</verification>

<success_criteria>
- All local code and configuration changes for dual-variant builds are in place
- CPU build command: `cargo tauri build --config src-tauri/tauri.cpu.conf.json` (no CUDA linkage)
- GPU build command: `cargo tauri build --features cuda --config src-tauri/tauri.gpu.conf.json` (CUDA enabled)
- Each binary points to its own updater manifest
- GPU installer has NVIDIA driver pre-check
- get_build_variant IPC command available for frontend
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-dual-cpu-gpu-installers-with-variant-specific-auto-updates/20-01-SUMMARY.md`
</output>
