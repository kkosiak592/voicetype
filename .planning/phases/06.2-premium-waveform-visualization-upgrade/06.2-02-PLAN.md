---
phase: 06.2-premium-waveform-visualization-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["06.2-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Neon waveform looks premium and JARVIS-like during recording"
    - "Waveform reacts visibly to voice — different speech sounds produce different shapes"
    - "No visual regression in processing or exit states"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the premium waveform visualization upgrade.

Purpose: Confirm the JARVIS-style neon waveform renders correctly, reacts to voice input, and maintains visual quality of existing states.

Output: Human-verified confirmation that the visualization meets premium quality bar.
</objective>

<execution_context>
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-premium-waveform-visualization-upgrade/06.2-01-SUMMARY.md
@.planning/phases/06.2-premium-waveform-visualization-upgrade/06.2-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify premium waveform visualization</name>
  <files>src/components/WaveformCanvas.tsx, src/Pill.tsx, src-tauri/src/pill.rs</files>
  <action>
    Human visual verification of Plan 01 output.

    What was built: Canvas2D neon waveform with layered bloom, 16-bin FFT voice reactivity, cyan-purple color palette, mirrored bezier curves, and updated recording border.

    How to verify:
    1. Run `cd src-tauri && cargo tauri dev` to start the app
    2. Recording state — waveform appearance:
       - Hold the hotkey to start recording
       - Verify 3 layered bezier curves appear with neon cyan-to-purple gradient
       - Verify thick bloom glow halos around the curves (JARVIS-style, not thin lines)
       - Verify bell-curve envelope (center peaks taller, edges taper)
    3. Recording state — voice reactivity:
       - Speak into the microphone at varying volumes
       - Verify waveform amplitude responds to voice level
       - Verify different speech sounds produce visibly different curve shapes (FFT frequency data)
       - Verify silence shows a very dim gentle wave (not dead flat line, not noisy jitter)
    4. Recording border:
       - Verify the rotating border around the pill uses cyan-purple gradient (not rainbow)
       - Verify border rotation animation is smooth
    5. Processing state (unchanged):
       - Release the hotkey to trigger processing
       - Verify orbiting plasma orbs still appear correctly
       - Verify processing border gradient still rotates
    6. Entrance/exit animations (unchanged):
       - Verify pill scales up smoothly on entrance
       - Verify pill scales down and fades on exit
    7. Performance:
       - Open DevTools (F12), go to Performance tab, record during a dictation
       - Verify frame times stay under 4ms (well within 16ms budget)

    Resume signal: Type "approved" if all checks pass, or describe specific visual issues to address.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human visual verification"</automated>
  </verify>
  <done>Human confirms waveform is premium/JARVIS-like, voice-reactive, and no regressions in processing or exit animations</done>
</task>

</tasks>

<verification>
Human confirms all visual criteria from the how-to-verify checklist.
</verification>

<success_criteria>
- Human approves waveform appearance as premium/JARVIS-like
- Voice reactivity confirmed — waveform responds to speech
- No regression in processing, entrance, or exit animations
- Performance confirmed acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-premium-waveform-visualization-upgrade/06.2-02-SUMMARY.md`
</output>
