---
phase: 06.1-fix-duplicate-tray-icons-and-replace-default-square-icon-with-proper-app-icon
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Only one tray icon visible in Windows notification area (not two)"
    - "Tray icon is the new VoiceType microphone design, not the Tauri grey square"
    - "Tray icon changes color when recording (red) and processing (orange)"
    - "Tooltip shows 'VoiceType - Idle' on hover when idle"
    - "Taskbar icon shows the new VoiceType icon (requires dev server restart)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 6.1 changes: confirm single tray icon (no duplicate), correct VoiceType branding on all surfaces, and tooltip feedback.

Purpose: Visual and functional verification that cannot be automated — requires a human to inspect the Windows notification area, taskbar, and hover behavior.

Output: Verified Phase 6.1 complete, or issues identified for correction.
</objective>

<execution_context>
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-fix-duplicate-tray-icons-and-replace-default-square-icon-with-proper-app-icon/06.1-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify tray icon, app icon, and tooltip behavior</name>
  <files></files>
  <action>
    Human verification checkpoint — no automated action. Plan 06.1-01 replaced all Tauri default icons with a custom VoiceType microphone icon, fixed the duplicate tray icon bug (replaced `default_window_icon()` with `Image::from_bytes(ICON_IDLE)` in `build_tray()`), redesigned tray state icons as colored microphone silhouettes, and added tooltip support showing "VoiceType - {State}".
  </action>
  <what-built>
    Plan 06.1-01 replaced all Tauri default icons with a custom VoiceType microphone icon, fixed the duplicate tray icon bug (replaced `default_window_icon()` with `Image::from_bytes(ICON_IDLE)` in `build_tray()`), redesigned tray state icons as colored microphone silhouettes, and added tooltip support showing "VoiceType - {State}".
  </what-built>
  <how-to-verify>
    **Prerequisites:** Restart the dev server (`npx vite build && cargo tauri dev`) to pick up new icon resources.

    **1. Single tray icon (CRITICAL)**
    - Look at the Windows notification area (system tray, bottom-right)
    - Confirm ONLY ONE VoiceType icon appears (not two)
    - The icon should be a microphone silhouette in indigo/purple on a dark background
    - If you see a grey square icon alongside a colored icon, the duplicate bug is NOT fixed

    **2. Tray icon state changes**
    - Hold the hotkey (Ctrl+Shift+Space or your configured key) to start recording
    - Observe tray icon changes to RED microphone silhouette
    - Release the hotkey — icon should briefly change to ORANGE/AMBER (processing)
    - After transcription completes, icon returns to INDIGO (idle)

    **3. Tooltip**
    - Hover mouse over the tray icon — should show "VoiceType - Idle"
    - While recording (hold hotkey), hover again — should show "VoiceType - Recording"
    - During processing, hover — should show "VoiceType - Processing"

    **4. Taskbar and title bar icon**
    - Open the Settings window (right-click tray > Settings)
    - Check the window title bar icon — should be the new VoiceType microphone, not the Tauri default
    - Check the Windows taskbar entry (if visible) — should also show the new icon
    - Note: In dev mode, the taskbar icon may not update until a full rebuild. If taskbar still shows default, this is acceptable for dev mode.

    **5. Ghost icon check**
    - If any ghost/stale tray icons from previous runs are visible, hover over them — they should disappear (this is normal Windows behavior, not a bug)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human visual verification required"</automated>
  </verify>
  <done>User confirms single tray icon, correct VoiceType icon design, state color changes, tooltip behavior, and taskbar/title bar icon</done>
  <resume-signal>Type "approved" if all checks pass, or describe which check(s) failed with details</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
1. Single tray icon (no duplicate)
2. Correct icon design (microphone, not grey square)
3. State-appropriate icon color changes work
4. Tooltip displays correctly with state
5. App icon visible on taskbar/title bar
</verification>

<success_criteria>
- User confirms single tray icon with VoiceType microphone design
- User confirms tray icon color changes during recording and processing
- User confirms tooltip shows "VoiceType - {State}"
- User confirms taskbar/title bar icon is updated (or accepts dev-mode limitation)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-fix-duplicate-tray-icons-and-replace-default-square-icon-with-proper-app-icon/06.1-02-SUMMARY.md`
</output>
