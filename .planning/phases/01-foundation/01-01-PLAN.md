---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/main.rs
  - src-tauri/src/tray.rs
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/icons/tray-icon.png
  - src/main.tsx
  - src/App.tsx
  - src/styles.css
  - package.json
  - tailwind.config.js
autonomous: true
requirements:
  - UI-05

must_haves:
  truths:
    - "App icon appears in the system tray after launch"
    - "Right-click on tray icon shows context menu with Settings and Quit"
    - "Clicking Quit in tray menu exits the app"
    - "Clicking Settings in tray menu opens the settings window"
    - "Closing the settings window hides it to tray instead of exiting the app"
    - "Launching a second instance focuses the existing settings window"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri builder with plugin registration, window event handling"
      contains: "tauri::Builder"
    - path: "src-tauri/src/tray.rs"
      provides: "System tray builder with two-item context menu"
      contains: "TrayIconBuilder"
    - path: "src-tauri/tauri.conf.json"
      provides: "Window definitions and app metadata"
      contains: "settings"
    - path: "src-tauri/capabilities/default.json"
      provides: "Plugin permission grants"
      contains: "permissions"
  key_links:
    - from: "src-tauri/src/tray.rs"
      to: "settings window"
      via: "on_menu_event show/set_focus"
      pattern: "get_webview_window.*settings"
    - from: "src-tauri/src/lib.rs"
      to: "tray.rs"
      via: "build_tray() call in setup()"
      pattern: "build_tray"
    - from: "src-tauri/src/lib.rs"
      to: "settings window"
      via: "on_window_event CloseRequested prevent_close"
      pattern: "prevent_close"
---

<objective>
Scaffold a Tauri 2.0 app with a system tray, settings window (hidden on startup), single-instance enforcement, and hide-to-tray on close. This is the empty container that all future phases build on.

Purpose: Establish the verified app shell before wiring hotkeys or persistence.
Output: Running Tauri app with tray icon, context menu (Settings/Quit), and settings window that hides on close.
</objective>

<execution_context>
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kkosiak.TITANPC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2.0 project with plugins and tray</name>
  <files>
    src-tauri/src/lib.rs
    src-tauri/src/main.rs
    src-tauri/src/tray.rs
    src-tauri/tauri.conf.json
    src-tauri/Cargo.toml
    src-tauri/capabilities/default.json
    src-tauri/icons/tray-icon.png
    package.json
    src/main.tsx
    src/App.tsx
    src/styles.css
    tailwind.config.js
  </files>
  <action>
    1. Create Tauri 2.0 project using `npm create tauri-app@latest` — choose React + TypeScript template. Run from the project root (C:\Users\kkosiak.TITANPC\Desktop\Code\voice-to-text). If the scaffolder requires a directory name, use `.` (current dir) or move files after.

    2. Install Tailwind CSS: `npm install -D tailwindcss @tailwindcss/vite` and configure for the src directory. Use Tailwind v4 with `@import "tailwindcss"` in the main CSS file if v4 is the version installed, or v3 config if that's what's available. Check the installed version.

    3. Add Tauri plugins:
       ```
       cargo tauri add single-instance
       cargo tauri add store
       cargo tauri add autostart
       ```
       Do NOT add global-shortcut yet (Plan 01-02 handles that).

    4. Ensure `Cargo.toml` has the `tray-icon` feature on the tauri dependency:
       ```toml
       tauri = { version = "2", features = ["tray-icon"] }
       ```

    5. Create `src-tauri/src/tray.rs` with the system tray builder:
       - Two menu items: "Settings" (id: "settings") and "Quit" (id: "quit")
       - `menu_on_left_click(false)` — left-click does nothing per user decision
       - on_menu_event: "settings" shows and focuses the settings window; "quit" calls `app.exit(0)`
       - Use the app's default icon for now (a microphone PNG will be added later or use default)
       - Export a `pub fn build_tray(app: &tauri::App) -> tauri::Result<()>` function

    6. Configure `src-tauri/src/lib.rs`:
       - Register plugins in this order: single-instance FIRST (before setup), then store, then autostart
       - Single-instance callback: show and focus "settings" window if it exists
       - setup() closure: call `build_tray(app)?`
       - on_window_event: intercept CloseRequested for "settings" window — call `window.hide()` + `api.prevent_close()`
       - Import tray module: `mod tray;`

    7. Configure `src-tauri/tauri.conf.json`:
       - One window: label "settings", title "VoiceType Settings", width 480, height 400, resizable false, visible false (starts hidden — tray is the entry point)
       - App identifier: something like "com.voicetype.app"

    8. Configure `src-tauri/capabilities/default.json` with permissions:
       ```json
       {
         "$schema": "../gen/schemas/desktop-schema.json",
         "identifier": "default",
         "description": "default permissions",
         "windows": ["settings"],
         "permissions": [
           "core:default",
           "store:default",
           "autostart:allow-enable",
           "autostart:allow-disable",
           "autostart:allow-is-enabled"
         ]
       }
       ```

    9. Create a minimal `src/App.tsx` that renders a centered "VoiceType Settings" heading with Tailwind classes. This is a placeholder — Plan 01-03 builds the real settings UI.

    10. Create a simple 32x32 transparent PNG for the tray icon. Use the Tauri default icon if generating a PNG is not feasible — the microphone silhouette is a visual polish concern, not a functional requirement.

    11. Run `cargo tauri dev` to verify:
        - App compiles and launches
        - Tray icon appears in system tray
        - Right-click shows Settings and Quit menu items
        - Clicking Settings opens the settings window
        - Closing settings window hides it (app stays in tray)
        - Clicking Quit exits the app
        - Launching a second instance focuses the existing window
  </action>
  <verify>
    <automated>cd C:/Users/kkosiak.TITANPC/Desktop/Code/voice-to-text && cargo tauri build --debug 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Tauri app compiles without errors
    - Tray icon visible in system tray on launch
    - Right-click context menu shows "Settings" and "Quit"
    - Settings click opens the settings window; Quit click exits
    - Closing settings window hides to tray (app continues running)
    - Second instance focuses existing settings window
  </done>
</task>

</tasks>

<verification>
1. `cargo tauri build --debug` completes without errors
2. Run the debug binary — tray icon appears
3. Right-click tray icon — menu shows Settings and Quit
4. Click Settings — window opens with "VoiceType Settings" heading
5. Close settings window (X button) — window hides, tray icon remains
6. Click Settings again — window reappears
7. Click Quit — app exits
</verification>

<success_criteria>
- Tauri 2.0 app compiles and runs on Windows
- System tray icon with working context menu (Settings, Quit)
- Settings window hides on close instead of exiting app
- Single-instance enforcement works
- All three plugins (single-instance, store, autostart) registered without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
